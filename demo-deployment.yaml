apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "corner-demo"
  namespace: "default"
  labels:
    app: "corner-demo"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "corner-demo"
  template:
    metadata:
      labels:
        app: "corner-demo"
    spec:
      containers:
      - name: "hellospring-sha256-1"
        image: asia-docker.pkg.dev/hopeful-ally-360404/test/hellospring:latest
---
apiVersion: "autoscaling/v2beta1"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "corner-demo-hpa-besk"
  namespace: "default"
  labels:
    app: "corner-demo"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "corner-demo"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 1
